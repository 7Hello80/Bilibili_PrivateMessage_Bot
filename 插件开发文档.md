# B站私信机器人 · 插件开发文档

> 版本：适用于插件系统全面升级版（2026-09）
> 本文档面向插件开发者，覆盖从入门到发布的全部知识点。

---

## 目录

1. [系统架构与插件运行原理](#一系统架构与插件运行原理)
2. [快速开始：第一个插件](#二快速开始第一个插件)
3. [插件目录结构与 package.json](#三插件目录结构与-packagejson)
4. [插件生命周期](#四插件生命周期)
5. [开发能力 API 详解](#五开发能力-api-详解)
6. [消息处理与命令](#六消息处理与命令)
7. [事件总线](#七事件总线)
8. [插件 API 接口](#八插件-api-接口)
9. [指标与仪表盘](#九指标与仪表盘)
10. [面板操作与插件的联动](#十面板操作与插件的联动)
11. [调试与排查](#十一调试与排查)
12. [发布到 GitHub 插件市场](#十二发布到-github-插件市场)
13. [安全须知](#十三安全须知)
14. [完整示例插件](#十四完整示例插件)
15. [常见问题 FAQ](#十五常见问题-faq)

---

## 一、系统架构与插件运行原理

```
┌─────────────────── 管理面板(web_panel.py, :5000) ───────────────────┐
│  文件管理/安装/备份/校验(只编译不执行)                                 │
│  写 plugin_control.json ──────┐                                      │
│  读 plugin_status.json ◄──────┼───────────────┐                     │
│  代理插件 API / 指标 ◄──── HTTP(127.0.0.1:9528)┤                     │
└───────────────────────────────┼───────────────┼─────────────────────┘
                                │               │
                    ┌───────────▼───────────────▼──────────────────────┐
                    │          机器人进程(index.py)                    │
                    │  · 插件加载器(PluginLoader)                      │
                    │  · 插件实例 × N(与机器人同进程)                   │
                    │  · 控制命令轮询线程(每2秒)                        │
                    │  · 插件 API 服务器(仅 127.0.0.1:9528, token鉴权)  │
                    │  · 事件总线(向所有插件广播事件)                   │
                    └──────────────────────────────────────────────────┘
```

**关键理解**：

1. **面板和机器人是两个独立进程**。插件代码只在机器人进程内执行；面板进程负责文件管理（创建/安装/备份/编辑），**永不执行插件代码**（语法检查用的是 `py_compile`，只编译不执行）。
2. **面板操作实时生效**：面板的启用/禁用/重载/配置保存会写入 `plugin_control.json` 控制文件，机器人每 2 秒轮询并执行，然后通过 `plugin_status.json` 回写状态快照（面板据此显示"运行中/未加载/加载失败"等真实状态）。
3. **消息处理流程**：机器人轮询到新私信 → 广播 `message_received` 事件 → 按 `load_order` 顺序让每个已加载插件处理（`process_message`）→ 若插件返回回复文本则使用之 → 否则回退到账号配置的关键词回复 → 检查"是否关注"限制（插件可声明 `bypass_follow_check` 绕过）→ 发送回复 → 广播 `message_sent` 事件。

---

## 二、快速开始：第一个插件

### 方式 1：面板创建（推荐）

管理面板 → 插件商店 → 创建插件：

| 类型 | 适用场景 |
|---|---|
| 基础(base) | 什么都不做的最小骨架 |
| 消息处理(message) | 关键词回复、命令处理 |
| 事件处理(event) | 监听 bot_start / 新粉丝等事件 |
| API扩展(api) | 对外提供 HTTP 接口 |
| 数据分析(analysis) | 统计、指标采集 |

创建后目录为 `plugins/bilibot_plugins_<插件名>/`，可立即在面板"插件详情 → 源码"中继续编辑。

### 方式 2：手动创建

最小的插件只需两个文件。目录结构：

```
plugins/
└── bilibot_plugins_你的插件名/     # 目录名 = 插件名(唯一标识)
    ├── package.json               # 插件元数据(必需)
    ├── main.py                    # 插件代码(必需, 必须包含 Plugin 类)
    ├── config.json                # 插件配置(可选, 面板可在线编辑)
    └── README.md                  # 说明文档(可选, 面板详情页展示)
```

**最小的 main.py**：

```python
import plugin_dev

class Plugin(plugin_dev.PluginBase):
    def __init__(self, bot_manager=None, config_manager=None, plugin_config=None):
        super().__init__(bot_manager, config_manager, plugin_config)
        self.version = "1.0.0"
        # 在这里注册命令/事件/API/指标
        self.register_command('hello', self.cmd_hello, '打招呼')

    def on_load(self):
        """插件加载时调用"""
        self.logger.info(f"插件 {self.name} 加载成功")

    def on_unload(self):
        """插件卸载时调用"""
        self.logger.info(f"插件 {self.name} 卸载成功")

    def cmd_hello(self, message_data, args):
        return "你好！"
```

**注意**：`on_load` 和 `on_unload` 是抽象方法，**必须实现**，否则加载会报错。

---

## 三、插件目录结构与 package.json

### package.json 字段详解

```json
{
    "name": "bilibot_plugins_demo",
    "version": "1.2.3",
    "description": "演示插件：展示全部插件能力",
    "author": "你的名字",
    "type": "message",
    "repository": "https://github.com/用户名/仓库名",
    "license": "MIT",
    "enabled": true,
    "load_order": 0,
    "dependencies": [],
    "bypass_follow_check": false
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | 插件名，**必须与目录名一致**（以 `bilibot_plugins_` 开头） |
| `version` | string | ✅ | 版本号，形如 `1.2.3`。面板"检查更新"按数字逐段比较 |
| `description` | string | ✅ | 功能描述，展示在插件卡片和详情页 |
| `author` | string | ✅ | 作者名 |
| `type` | string | - | 插件类型：`base` / `message` / `event` / `api` / `analysis`（仅展示用途，不影响运行） |
| `repository` | string | - | GitHub 仓库地址。**面板"检查更新/一键更新"依赖此字段** |
| `license` | string | - | 许可证，默认 MIT |
| `enabled` | bool | ✅ | 是否启用。**由面板维护，插件代码不要自行修改此文件** |
| `load_order` | int | - | 加载顺序，数字小者先加载。消息按此顺序交给各插件处理，**想抢在别的插件前处理消息就设小一点** |
| `dependencies` | array | - | 依赖声明，两种语义，见下文 |
| `bypass_follow_check` | bool | - | `true` 时本插件的回复**绕过"必须关注才能回复"检查**（未关注用户也能收到回复） |

### dependencies 两种语义

系统自动判断每个依赖项属于哪一类：

1. **插件依赖**：`plugins/<依赖名>/package.json` 存在 → 视为依赖另一个插件，要求该插件已启用并加载成功，否则本插件**拒绝加载**（面板显示失败原因）。
2. **Python 模块依赖**：找不到同名插件目录 → 用 `importlib.util.find_spec` 探测 Python 模块，未安装则拒绝加载。

```json
"dependencies": ["bilibot_plugins_stats", "requests"]
```

> 依赖缺失时插件会留在列表中，状态显示"加载失败"，点开可看到具体原因（如 `依赖不满足: Python 模块 requests 未安装`）。

---

## 四、插件生命周期

### 加载流程（load）

1. 读取 `package.json`，校验依赖（不满足则记录 `load_error` 并跳过）；
2. `importlib` 动态导入 `main.py`（模块名 `plugins.<目录名>`）；
3. 实例化 `Plugin` 类，传入三个参数：
   - `bot_manager`：BotManager 实例（可通过 `self.bot_manager` 访问，含 `.bots` 账号列表）
   - `config_manager`：全局配置管理器（账号/关键词配置）
   - `plugin_config`：**package.json 的元数据字典**（`self.plugin_config`）
4. 调用 `on_load()`。若 `on_load` 抛异常，插件加载失败并清理模块。

### 卸载流程（unload）

1. 调用 `on_unload()`（异常会被记录但不中断）；
2. **兜底**：加载器自动调用 `scheduler.stop_all()` 停止本插件的全部定时线程（即使插件忘了手动停止）；
3. 从 `sys.modules` 移除插件模块。

### 重载（reload）

卸载 + 重新加载。重载后 `on_load` 会再次执行，`__init__` 里注册的所有处理器/命令/事件**重新注册**（不存在重复注册问题，因为实例是全新的）。

### 生命周期约定

| 时机 | 该做什么 |
|---|---|
| `on_load` | 启动定时任务、初始化资源、读取配置 |
| `on_unload` | 释放资源、停止定时任务（可调用 `self.scheduler.stop_all()`；不调用也会被兜底） |

> ⚠️ 定时任务若不停止，插件卸载/重载后旧线程仍在后台运行（旧版系统的典型问题）。新版已兜底，但仍建议在 `on_unload` 显式调用 `stop_all()`。

---

## 五、开发能力 API 详解

所有工具类在 `PluginBase.__init__` 中自动初始化，直接通过 `self.xxx` 使用。

### 5.1 self.logger — 日志

```python
self.logger.info("普通信息")
self.logger.error("错误信息")
self.logger.warning("警告信息")
self.logger.debug("调试信息")
```

- 日志写入 `plugins/<插件名>/plugin.log`（自动轮转：512KB × 2 份），带时间戳和级别。
- 面板"插件详情 → 日志"页可直接查看，也可在服务器上 `cat plugins/<插件名>/plugin.log`。
- 日志不与机器人主日志混写，互不干扰。

### 5.2 self.config — 配置

持久化于 `plugins/<插件名>/config.json`，面板"插件详情 → 配置"页可直接编辑。

| 方法 | 说明 |
|---|---|
| `get(key, default=None)` | 读取单个配置项 |
| `get_all()` | 返回全部配置的拷贝 |
| `set(key, value)` | 写入单项并立即保存 |
| `update(dict)` | 批量合并并保存，返回是否成功 |
| `delete(key)` | 删除单项并保存 |

```python
# 读取(带默认值)
cooldown = self.config.get('cooldown_seconds', 60)

# 运行时保存
self.config.set('last_run_at', self.utils.format_time())
```

> **配置保存的联动机制**：面板保存配置时，若机器人正在运行，会通过 IPC 命令让**机器人进程**写文件并自动重载插件（避免双进程写文件竞态）；机器人未运行时由面板直接写文件，下次启动生效。

### 5.3 self.database — SQLite 数据库

数据文件 `plugins/<插件名>/<插件名>.db`。

| 方法 | 说明 |
|---|---|
| `create_table(name, {列名: 类型})` | 建表（IF NOT EXISTS） |
| `execute(sql, params=())` | 执行写操作（自动提交） |
| `fetch_all(sql, params=())` | 查询全部 |
| `fetch_one(sql, params=())` | 查询单行 |
| `get_connection()` | 获取原生连接 |

**上下文管理器**（推荐写事务用）：

```python
with self.database as conn:
    conn.execute('INSERT INTO t (a, b) VALUES (?, ?)', (1, 2))
    # 正常退出自动 commit，异常自动 rollback
```

示例：

```python
self.database.create_table('message_log', {
    'id': 'INTEGER PRIMARY KEY AUTOINCREMENT',
    'sender_uid': 'TEXT',
    'content': 'TEXT',
    'created_at': 'TEXT'
})
self.database.execute(
    'INSERT INTO message_log (sender_uid, content, created_at) VALUES (?, ?, ?)',
    (uid, text, self.utils.format_time()))
row = self.database.fetch_one('SELECT COUNT(*) FROM message_log')
```

### 5.4 self.cache — 缓存（带 TTL）

内存缓存 + JSON 落盘（`plugins/<插件名>/cache.json`），TTL 到期自动失效。

| 方法 | 说明 |
|---|---|
| `get(key, default=None)` | 读取（已过期返回 default 并删除） |
| `set(key, value, ttl=0)` | 写入；`ttl` 为过期秒数，`0` 表示永不过期 |
| `delete(key)` | 删除单项 |
| `clear()` | 清空 |

**典型用法：防刷屏冷却**（同一用户 N 秒内只回复一次）：

```python
def handle_message(self, message_data):
    uid = str(message_data.get('sender_uid', ''))
    if self.cache.get(f"cooldown_{uid}"):
        return None                      # 冷却期内不回复
    self.cache.set(f"cooldown_{uid}", 1, ttl=60)
    return "欢迎使用！"
```

### 5.5 self.http — HTTP 客户端

| 方法 | 说明 |
|---|---|
| `get(url, **kwargs)` | GET，失败抛异常（30 秒超时） |
| `post(url, **kwargs)` | POST |
| `get_json(url, **kwargs)` | GET 并解析 JSON，失败返回 `None`（不抛） |
| `post_json(url, **kwargs)` | POST 并解析 JSON，失败返回 `None` |

```python
data = self.http.get_json("https://api.example.com/info")
if data:
    self.logger.info(f"获取到: {data}")
```

### 5.6 self.scheduler — 定时任务

| 方法 | 说明 |
|---|---|
| `schedule_interval(seconds, func, *args, **kwargs)` | 周期性任务：**先执行一次**，之后每隔 `seconds` 秒执行 |
| `schedule_once(delay, func, *args, **kwargs)` | 延迟 `delay` 秒后执行一次 |
| `stop_all()` | 停止全部任务（Event 通知 + 最多等 2 秒） |

```python
def on_load(self):
    self.scheduler.schedule_interval(3600, self.hourly_task)  # 每小时
    self.scheduler.schedule_once(10, self.startup_task)       # 10秒后一次

def on_unload(self):
    self.scheduler.stop_all()   # 卸载时停止, 防止后台残留

def hourly_task(self):
    self.send_message(123456, "整点提醒！")
```

> - 任务异常会被捕获并记录日志，不会中断调度循环。
> - 任务是 daemon 线程；`stop_all()` 后线程在下一个等待周期退出（最多残留 2 秒）。

### 5.7 self.utils — 工具函数

| 方法 | 说明 |
|---|---|
| `format_time(ts=None)` | 时间戳 → `YYYY-mm-dd HH:MM:SS` |
| `md5(text)` | 计算 MD5 |
| `safe_json_loads(text, default=None)` | 安全解析 JSON（失败返回默认值） |
| `chunk_list(lst, size)` | 列表分块 |
| `format_file_size(size)` | 字节 → 人类可读大小 |
| `parse_version("1.2.3")` | 版本号 → 元组 `(1,2,3)`，用于版本比较 |

### 5.8 机器人交互

| 方法 | 说明 |
|---|---|
| `send_message(receiver_id, message, account_index=0)` | 用指定账号（默认第一个）发送私信 |
| `reply(message_data, text, account_index=0)` | **快捷回复**：自动从 message_data 取出 sender 发回 |
| `get_user_info(user_id, account_index=0)` | 查询 B 站用户信息（昵称等） |
| `get_bot_accounts()` | 获取所有机器人账号实例列表 |

```python
def handle_hi(self, message_data):
    # 等价于 send_message(message_data['talker_id'], '你好')
    return self.reply(message_data, '你好')
```

> `account_index` 对应 config.json 中账号的顺序（0 = 第一个）。多账号场景注意选择正确的账号回话。

---

## 六、消息处理与命令

### 6.1 message_data 字段

插件收到的消息数据结构（`message_received` 事件与 `process_message` 参数相同）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `talker_id` | int | 会话对象 UID（通常即发送者 UID） |
| `sender_uid` | int/str | 发送者 UID |
| `content` | str | 纯文本消息内容 |
| `timestamp` | int | 消息时间戳（秒） |
| `msg_id` | int | 消息序号 |
| `account_name` | str | 收到消息的机器人账号名 |

### 6.2 消息处理器

```python
# __init__ 中注册, 可注册多个, 按注册顺序依次尝试
self.register_message_handler(self.handle_keywords)

def handle_keywords(self, message_data):
    content = message_data.get('content', '')
    if '你好' in content:
        return '你好呀~'
    return None   # 返回 None 表示"我不处理", 交给下一个处理器/插件
```

- 返回**非 None 的字符串**即作为回复发送（自动走关注检查与事件广播）。
- 返回 `None` 表示不处理，继续下一个处理器。
- 处理器内部抛异常会被捕获并记录，不影响其他处理器。

### 6.3 命令处理器（`!` 前缀）

```python
self.register_command('stats', self.cmd_stats, '查看统计')

def cmd_stats(self, message_data, args):
    # 用户发送 "!stats 今天" 时, args = "今天"
    return "统计结果..."
```

- 用户发送 `!stats`（大小写不敏感）触发；`!stats xxx` 时 `xxx` 作为 `args` 参数。
- 命令优先级**高于**消息处理器：以 `!` 开头的消息先查命令，未命中再走消息处理器。
- 用户发送 **`!help`** 时，机器人自动聚合**所有已加载插件**的命令列表返回（无需自己实现帮助命令）。
- 面板"插件详情 → 信息"页展示本插件已注册命令；命令描述越清晰越好。

### 6.4 处理顺序与回复优先级

1. 机器人先广播 `message_received` 事件（所有插件的事件处理器都会执行，**无法阻止回复**）；
2. 按 `load_order` 从小到大依次调用各插件的 `process_message`，**第一个返回非 None 的插件胜出**，其回复作为最终回复；
3. 所有插件都返回 None → 回退到账号配置的关键词回复（config.json 的 keyword 字段）；
4. 最终回复前检查"是否关注"限制：
   - 获胜插件声明了 `bypass_follow_check: true` → 直接发送；
   - 否则用户必须已关注机器人（或账号开启了 `no_focus_hf`）才发送；
5. 发送成功后广播 `message_sent` 事件。

---

## 七、事件总线

机器人运行时向所有插件广播事件。用 `register_event_handler` 订阅：

| 事件 | 触发时机 | 载荷(data) 关键字段 |
|---|---|---|
| `bot_start` | 机器人启动、插件全部加载完成后 | `accounts`(账号名列表), `time` |
| `bot_stop` | 机器人停止前 | `time` |
| `message_received` | 收到新私信、**插件处理之前** | `talker_id`, `sender_uid`, `content`, `timestamp`, `msg_id`, `account_name` |
| `message_sent` | 成功发送一条消息后 | `receiver_id`, `content`, `account_name`, `is_image`(图片时为 true) |
| `new_follower` | 发现新关注用户并发送欢迎消息后 | `mid`, `uname`, `account_name` |

```python
self.register_event_handler('message_received', self.on_message)
self.register_event_handler('new_follower', self.on_new_follower)

def on_message(self, message_data):
    self.logger.info(f"收到 {message_data.get('sender_uid')} 的消息")

def on_new_follower(self, data):
    self.send_message(data['mid'], f"欢迎 {data['uname']} 关注！")
```

**注意事项**：

- 事件回调运行在机器人轮询线程上，**注意线程安全**（多个账号的机器人共享同一事件总线，回调可能并发执行）。
- 回调返回值被忽略——想回复消息请用消息处理器/命令，事件只用于"感知"和副作用（统计、通知等）。
- 单个插件的回调异常不会影响其他插件（异常会被记录）。

---

## 八、插件 API 接口

### 注册

```python
self.register_api_route('/stats', self.api_stats)                       # 默认 GET
self.register_api_route('/report', self.api_report, methods=['GET', 'POST'])
```

### 访问方式

面板登录后经反向代理访问（机器人进程内的服务只监听 127.0.0.1，需 token 鉴权，不对外网开放）：

```
GET/POST  /api/plugins/api/<插件名>/<路由路径>
```

例如：`GET /api/plugins/api/bilibot_plugins_stats/stats`

**面板内可直接测试**：插件详情 → "API测试" tab，输入路径、选 GET/POST、填 JSON 请求体，一键发送查看响应。

### 处理器签名与数据传递

```python
def api_stats(self, data):
    # data: POST 请求体(JSON 解析后) + URL query 参数合并
    return {'success': True, 'total': 100}
```

- `data` 为 dict：POST 的 JSON body 与 `?key=value` query 参数合并。
- 返回 dict → 作为 JSON 返回（状态码 200）；返回其他类型 → 包一层 `{'success': True, 'data': ...}`；返回 None → 404"路由不存在"。
- 处理器抛异常 → 500 + 错误信息。

**约定**：建议统一返回 `{'success': True/False, 'message': ..., ...}` 结构，方便前端和调试。

---

## 九、指标与仪表盘

### 注册指标

```python
self.register_metric('total_messages', self.metric_total)
self.register_metric('uptime', self.metric_uptime)

def metric_total(self):
    return 100   # 返回值任意类型(数字/字符串/字典)
```

### 查看

- 面板"插件详情 → 指标"页：指标以**卡片**形式展示 + 原始仪表盘 JSON。
- 或直接访问 `GET /api/plugins/metrics?plugin=<插件名>`。

### 仪表盘数据

可覆盖 `create_dashboard_data()` 自定义仪表盘结构：

```python
def create_dashboard_data(self):
    return {
        'metrics': self.collect_metrics(),
        'extra': {'说明': '自定义字段'},
        'timestamp': self.utils.format_time(),
        'plugin': self.name
    }
```

---

## 十、面板操作与插件的联动

| 面板操作 | 实际发生了什么 |
|---|---|
| **启用/禁用** | 面板原子写 package.json 的 enabled → 发 IPC 命令 → 机器人加载/卸载插件（未运行时下次启动生效） |
| **重载** | 发 IPC 命令 → 机器人卸载+重载该插件 |
| **全部重载** | 机器人重载全部已加载插件，并尝试加载磁盘上新增的插件 |
| **保存配置** | 机器人运行时：IPC `set_config` 命令 → 机器人写 config.json 并重载插件；未运行时：面板直写文件 |
| **保存源码** | 面板校验（.py 编译检查 / .json 格式检查）→ 自动备份 → 写文件 → IPC 重载 |
| **备份/恢复** | 打包插件目录为时间戳 zip；恢复时自动备份当前版本再替换，随后重载 |
| **检查更新** | 读 package.json 的 `repository` 字段 → 拉取远程 package.json → `parse_version` 比较本地版本 |
| **更新** | 检查到新版本 → 卸载 → 自动备份 → 重新下载 → 保持 enabled 状态 → 加载 |
| **安装(zip/市场)** | 安全解压(zip-slip 防护) → 结构+语法校验 → 安装 → IPC 加载 |

---

## 十一、调试与排查

### 调试三板斧

1. **看日志**：`self.logger.info(...)` 打点 → 面板"插件详情 → 日志"页实时查看；
2. **API 测试**：把中间数据通过 `register_api_route` 暴露 → 面板"API测试"页直接调用；
3. **`!help`**：确认命令是否正确注册。

### 加载失败怎么办

面板插件列表/详情页会显示"加载失败"徽章，点击查看具体原因，常见的有：

| 错误信息 | 原因与处理 |
|---|---|
| `依赖不满足: Python 模块 xxx 未安装` | 服务器缺少该 pip 包，安装或在 dependencies 中移除 |
| `依赖不满足: 插件依赖 xxx 未加载` | 依赖的插件未启用或加载失败 |
| `没有找到 Plugin 类` | main.py 缺少 `class Plugin` |
| `Plugin 类缺少 on_load 方法`（结构校验时） | 未实现抽象方法 |
| `语法错误: ...` | main.py 有语法问题（面板保存时会提前拦住） |

### 本地快速验证（不通过面板）

```bash
cd 项目根目录
python3 -c "
import plugin_loader
loader = plugin_loader.PluginLoader('plugins')
loader.load_plugin('bilibot_plugins_你的插件')
plugin = loader.get_plugin('bilibot_plugins_你的插件')
print('加载状态:', plugin.instance is not None, '| 错误:', plugin.load_error)
# 模拟一条消息
print(plugin.instance.process_message({'content': '!hello', 'sender_uid': 1, 'talker_id': 1}))
plugin.unload()
"
```

---

## 十二、发布到 GitHub 插件市场

插件市场搜索 GitHub 上**仓库名以 `bilibot_plugins_` 开头**的公开仓库，按星标排序。

### 发布步骤

1. 创建仓库，命名 `bilibot_plugins_<插件名>`；
2. 仓库根目录直接放 `package.json` + `main.py`（+ 可选 config.json / README.md），**不要套一层子目录**；
3. 在 `package.json` 中填写 `repository` 字段指向该仓库（**更新检查必需**）；
4. 提交并 push。用户在面板"插件市场"搜索到后点击安装即可。

### 安装校验规则（都会被面板自动执行）

- zip 包路径安全（拒绝绝对路径/`..`/符号链接）；
- `package.json` 可解析且含 `name`、`version`；
- `main.py` 含 `class Plugin` 且 `py_compile` 编译通过（**只编译不执行**）；
- 插件名清洗（仅字母数字下划线连字符，自动补 `bilibot_plugins_` 前缀）。

### 更新机制

面板"检查更新"读取仓库默认分支的 `package.json` 版本号，与本地比较，**新版本号数字大于本地**才提示更新。发新版只需改 `version` 字段并 push。

---

## 十三、安全须知

1. **插件代码与机器人同进程运行**，可以访问账号凭证（SESSDATA 等）、全局配置和面板数据。**安装/导入插件即信任其作者**——只安装可信来源的插件。
2. 面板对插件只做结构与语法校验，**无法审查代码行为**（例如恶意插件可以读取并外传凭证）。
3. 插件 API 服务仅监听 `127.0.0.1:9528`，需动态 token 鉴权，不对公网开放；面板代理也要求登录。
4. 面板进程永不执行插件代码；源码在线编辑保存前仅做编译/格式检查。
5. 插件源码在线编辑接口有文件白名单（文本扩展名）+ 路径穿越防护 + 自动备份，但**能修改插件代码的人等于能控制机器人**——保护好面板管理员密码。
6. 发布开源插件前，检查自己的插件是否硬编码了任何密钥、token、私人 UID。

---

## 十四、完整示例插件

一个演示全部能力的"完整示例"（可对照学习每种 API 的用法）：

```python
# -*- coding: utf-8 -*-
"""
完整示例插件: 演示消息处理/命令/事件/API/指标/数据库/缓存/定时任务的综合用法
"""
import plugin_dev
import time


class Plugin(plugin_dev.PluginBase):
    def __init__(self, bot_manager=None, config_manager=None, plugin_config=None):
        super().__init__(bot_manager, config_manager, plugin_config)
        self.version = "1.0.0"
        self.start_time = time.time()

        # 1. 数据库
        self.database.create_table('hits', {
            'id': 'INTEGER PRIMARY KEY AUTOINCREMENT',
            'uid': 'TEXT',
            'keyword': 'TEXT',
            'created_at': 'TEXT'
        })

        # 2. 消息处理器(关键词回复 + 冷却)
        self.register_message_handler(self.handle_keywords)

        # 3. 命令
        self.register_command('demo', self.cmd_demo, '演示命令: !demo [文本]')
        self.register_command('count', self.cmd_count, '查询命中次数')

        # 4. 事件
        self.register_event_handler('bot_start', self.on_bot_start)
        self.register_event_handler('new_follower', self.on_new_follower)

        # 5. API
        self.register_api_route('/demo', self.api_demo)
        self.register_api_route('/count', self.api_count)

        # 6. 指标
        self.register_metric('hit_count', self.metric_hits)
        self.register_metric('uptime', lambda: int(time.time() - self.start_time))

    # ============ 生命周期 ============
    def on_load(self):
        self.logger.info(f"完整示例插件 {self.name} v{self.version} 加载成功")
        # 7. 定时任务(每 6 小时执行一次)
        self.scheduler.schedule_interval(6 * 3600, self.periodic_task)

    def on_unload(self):
        self.scheduler.stop_all()
        self.logger.info(f"完整示例插件 {self.name} 卸载成功")

    # ============ 消息处理 ============
    def handle_keywords(self, message_data):
        content = message_data.get('content', '')
        uid = str(message_data.get('sender_uid', ''))
        # 冷却: 每用户 60 秒内最多回复一次
        if self.cache.get(f"cooldown_{uid}"):
            return None
        for kw, reply in self.config.get('replies', {}).items():
            if kw in content:
                self.cache.set(f"cooldown_{uid}", 1, ttl=60)
                self._record(uid, kw)
                return reply
        return None

    # ============ 命令 ============
    def cmd_demo(self, message_data, args):
        return f"收到参数: {args or '(空)'}"

    def cmd_count(self, message_data, args):
        row = self.database.fetch_one('SELECT COUNT(*) FROM hits')
        return f"累计命中 {row[0] if row else 0} 次"

    # ============ 事件 ============
    def on_bot_start(self, data):
        self.logger.info(f"机器人启动, 账号: {data.get('accounts')}")

    def on_new_follower(self, data):
        self.logger.info(f"新粉丝: {data.get('uname')}({data.get('mid')})")
        self.send_message(data['mid'], "欢迎关注！")

    # ============ API ============
    def api_demo(self, data):
        return {'success': True, 'plugin': self.name,
                'version': self.version, 'params': data}

    def api_count(self, data):
        row = self.database.fetch_one('SELECT COUNT(*) FROM hits')
        return {'success': True, 'count': row[0] if row else 0}

    # ============ 指标 ============
    def metric_hits(self):
        row = self.database.fetch_one('SELECT COUNT(*) FROM hits')
        return row[0] if row else 0

    # ============ 定时任务 ============
    def periodic_task(self):
        self.logger.info("定时任务执行")
        # 例如给指定 UID 发通知(从配置读取)
        target = self.config.get('notify_uid', 0)
        if target:
            self.send_message(target, "定时提醒")

    # ============ 内部工具 ============
    def _record(self, uid, keyword):
        self.database.execute(
            'INSERT INTO hits (uid, keyword, created_at) VALUES (?, ?, ?)',
            (uid, keyword, self.utils.format_time()))
```

对应的 `config.json`：

```json
{
    "replies": {
        "你好": "你好呀~ [示例插件]",
        "在吗": "在的，有什么可以帮你？[示例插件]"
    },
    "notify_uid": 0
}
```

---

## 十五、常见问题 FAQ

**Q1：修改了插件代码，为什么不生效？**
在面板"插件详情 → 源码"保存会自动重载；若在服务器上直接改文件，需要点"重载"（或重启机器人）。

**Q2：配置保存后插件行为没变？**
检查 `on_load` 是否读取了配置——`self.config.get()` 在 `on_load` 中读取一次后若缓存到变量，重载后才会刷新；保存配置会自动触发重载，行为应当更新。若仍无效，查看插件日志确认重载是否成功。

**Q3：为什么我的回复没有发出去？**
检查三点：① 插件是否"运行中"；② 回复是否被更小 `load_order` 的插件抢先（返回了非 None）；③ 用户是否已关注机器人（除非声明了 `bypass_follow_check` 或账号开启了"不关注也可回复"）。

**Q4：多个账号的机器人会各自加载一份插件吗？**
不会。插件**只加载一份实例**，所有账号的消息都会经过同一批插件处理（消息数据里的 `account_name` 可区分账号）；`send_message` 的 `account_index` 用于选择用哪个账号发送。

**Q5：插件里能调用 requests 直接请求 B 站 API 吗？**
可以，但建议用 `self.http`（带插件 UA 与错误处理）；需要账号凭证时通过 `self.get_bot_accounts()` 获取账号实例（含 headers/cookie）。

**Q6：如何让插件只在某个账号生效？**
在消息处理器中判断 `message_data.get('account_name')`，不符合则返回 None。

**Q7：插件加载失败但面板显示"已启用"？**
状态徽章区分"启用(磁盘配置)"与"运行中(实际加载)"；加载失败会显示红色"加载失败"，点击查看原因。

**Q8：定时任务最短间隔是多少？**
理论上任意秒数，但建议 ≥60 秒（太频繁会占用轮询与网络资源）。

**Q9：插件可以读写其他插件的文件吗？**
技术上可以（同进程、同目录），但**不要这么做**——插件间请通过"插件依赖 + 公开的 API 路由"协作。

**Q10：如何彻底删除插件？**
面板卸载（自动备份到 `plugin_backups/`）→ 确认不需要后可删除备份；或直接删除 `plugins/<插件名>/` 目录后点"全部重载"。
