# B站私信机器人 · 插件开发文档

插件以「一个目录一个插件」的形式存放于 `plugins/` 目录。插件代码与机器人同进程运行，可调用机器人提供的完整开发 API（`plugin_dev` 模块）。

---

## 一、快速开始

### 方式 1：面板创建

管理面板 → 插件商店 → 创建插件，选择类型（基础/消息/事件/API/分析）即可生成插件骨架。

### 方式 2：手动创建目录结构

```
plugins/
└── bilibot_plugins_你的插件名/     # 目录名 = 插件名(唯一标识)
    ├── package.json               # 插件元数据(必需)
    ├── main.py                    # 插件代码(必需, 必须包含 Plugin 类)
    ├── config.json                # 插件配置(可选, 面板可在线编辑)
    └── README.md                  # 说明文档(可选, 面板详情页展示)
```

最小的 `main.py`：

```python
import plugin_dev

class Plugin(plugin_dev.PluginBase):
    def __init__(self, bot_manager=None, config_manager=None, plugin_config=None):
        super().__init__(bot_manager, config_manager, plugin_config)
        self.version = "1.0.0"
        self.register_command('hello', self.cmd_hello, '打招呼')

    def on_load(self):
        self.logger.info(f"插件 {self.name} 加载成功")

    def on_unload(self):
        self.logger.info(f"插件 {self.name} 卸载成功")

    def cmd_hello(self, message_data, args):
        return "你好！"
```

> 注意：插件**必须实现** `on_load` 和 `on_unload` 方法（基类声明为抽象方法）。

---

## 二、package.json 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `name` | string | ✅ | 插件名，与目录名一致 |
| `version` | string | ✅ | 版本号，如 `1.2.3`（用于更新检查比较） |
| `description` | string | ✅ | 功能描述 |
| `author` | string | ✅ | 作者 |
| `type` | string | - | 插件类型: base / message / event / api / analysis |
| `repository` | string | - | GitHub 仓库地址，如 `https://github.com/用户/仓库`（面板"检查更新"依赖此字段） |
| `license` | string | - | 许可证，默认 MIT |
| `enabled` | bool | ✅ | 是否启用（面板控制，插件不要自行修改） |
| `load_order` | int | - | 加载顺序，数字小的先加载（默认 0） |
| `dependencies` | array | - | 依赖声明，见下文 |
| `bypass_follow_check` | bool | - | `true` 时本插件的回复**绕过"必须关注才能回复"检查**，未关注用户也能收到回复 |

### 依赖声明（dependencies）

两种语义：

1. **插件依赖**：`plugins/<依赖名>/package.json` 存在 → 视为依赖另一个插件，要求该插件已启用并加载成功，否则本插件不加载（面板显示失败原因）。
2. **Python 模块依赖**：找不到同名插件目录 → 用 `importlib.util.find_spec` 探测，未安装则本插件不加载。

```json
"dependencies": ["bilibot_plugins_stats", "requests"]
```

---

## 三、生命周期

| 方法 | 时机 | 约定 |
|---|---|---|
| `on_load()` | 插件加载成功后 | 注册定时任务、初始化资源 |
| `on_unload()` | 插件卸载/重载前 | 释放资源；**使用 `self.scheduler` 时建议调用 `self.scheduler.stop_all()`**（即使忘记，加载器也会兜底停止） |

---

## 四、能力 API 速查

所有工具类在 `PluginBase.__init__` 中自动初始化，直接通过 `self.xxx` 使用：

| 属性 | 类 | 常用方法 |
|---|---|---|
| `self.logger` | `PluginLogger` | `info/error/warning/debug(msg)`，日志落盘 `plugins/<插件名>/plugin.log`（面板日志页可看） |
| `self.config` | `PluginConfig` | `get(key, default)` / `set(key, value)` / `get_all()` / `update(dict)` / `delete(key)`，持久化于 `plugins/<插件名>/config.json` |
| `self.database` | `PluginDatabase` | `execute(sql, params)` / `fetch_all` / `fetch_one` / `create_table`，SQLite 存储；支持 `with self.database as conn:` 上下文 |
| `self.cache` | `PluginCache` | `get(key, default)` / `set(key, value, ttl=秒)` / `delete` / `clear`，TTL 到期自动失效 |
| `self.http` | `PluginHTTPClient` | `get/post(url)` / `get_json/post_json(url)` |
| `self.scheduler` | `PluginScheduler` | `schedule_interval(秒, func)` / `schedule_once(秒, func)` / `stop_all()`，卸载时可真正停止 |
| `self.utils` | `PluginUtils` | `format_time()` / `md5()` / `chunk_list()` / `parse_version()` 等 |
| `self.version` | - | 插件版本号 |

### 机器人交互

```python
self.send_message(receiver_id, "你好", account_index=0)  # 指定账号发送
self.reply(message_data, "你好")                          # 快捷回复当前消息发送者
self.get_user_info(user_id)                              # 查询用户信息
self.get_bot_accounts()                                  # 获取所有机器人账号实例
```

---

## 五、消息处理与命令

### 消息处理器

```python
# __init__ 中注册, 返回非 None 文本即作为回复发送
self.register_message_handler(self.handle_keywords)

def handle_keywords(self, message_data):
    # message_data: {'talker_id', 'sender_uid', 'content', 'timestamp',
    #                'msg_id', 'account_name'}
    if '你好' in message_data.get('content', ''):
        return '你好呀~'
    return None
```

### 命令处理器（`!` 前缀）

```python
self.register_command('stats', self.cmd_stats, '查看统计')

def cmd_stats(self, message_data, args):   # args 为 !stats 之后的文本
    return "统计结果..."
```

- 用户发送 `!stats` 即触发。
- 用户发送 **`!help`** 时机器人自动聚合所有插件的已注册命令（名称+描述）返回。
- 面板插件详情页「信息」tab 也会展示已注册命令。

---

## 六、事件总线

机器人会在运行过程中广播以下事件，用 `register_event_handler` 订阅：

| 事件 | 触发时机 | 载荷(data) 关键字段 |
|---|---|---|
| `bot_start` | 机器人启动完成 | `accounts`(账号名列表), `time` |
| `bot_stop` | 机器人停止前 | `time` |
| `message_received` | 收到一条新私信（插件处理前） | `talker_id`, `sender_uid`, `content`, `timestamp`, `msg_id`, `account_name` |
| `message_sent` | 成功发送一条消息 | `receiver_id`, `content`, `account_name`, `is_image`(可选) |
| `new_follower` | 发现新关注用户并发送欢迎后 | `mid`, `uname`, `account_name` |

```python
self.register_event_handler('message_received', self.on_message)

def on_message(self, message_data):
    self.logger.info(f"收到 {message_data.get('sender_uid')} 的消息")
```

> 事件回调运行在机器人轮询线程中，注意线程安全。

---

## 七、插件 API 与指标

### 注册 API 路由

```python
self.register_api_route('/stats', self.api_stats)          # 默认 GET
self.register_api_route('/report', self.api_report, methods=['GET', 'POST'])

def api_stats(self, data):   # data: POST 的 JSON body + query 参数
    return {'success': True, 'total': 100}
```

访问方式（需登录面板）：`GET/POST /api/plugins/api/<插件名>/<路由路径>`

例如 `GET /api/plugins/api/bilibot_plugins_stats/stats`。机器人未运行时返回 503 提示。

### 注册指标

```python
self.register_metric('total_messages', self.metric_total)

def metric_total(self):
    return 100
```

面板「插件详情 → 指标」页实时展示（数据经机器人进程内 HTTP 服务代理，仅本机可访问）。

---

## 八、发布到 GitHub（插件市场）

插件市场搜索 GitHub 上**仓库名以 `bilibot_plugins_` 开头**的公开仓库，按星标排序。

1. 创建仓库，命名 `bilibot_plugins_你的插件名`；
2. 仓库根目录直接放 `package.json` + `main.py`（+可选 config.json/README.md）；
3. 在 `package.json` 中填写 `repository` 字段（指向该仓库，更新检查用）；
4. 用户在面板搜索到后点击安装即可（安装前会做结构校验与语法编译检查，**只编译不执行**）。

---

## 九、安全须知

1. **插件代码与机器人同进程运行**，可以访问账号凭证（SESSDATA 等）和全部配置。只安装/导入可信来源的插件。
2. 面板对插件 zip 包做 zip-slip 防护与语法校验，但无法审查代码行为。
3. 插件 API 服务仅监听 `127.0.0.1:9528`，需 token 鉴权，不对公网开放。
4. 面板进程**永不执行插件代码**；源码在线编辑器保存前仅做 `py_compile` 语法检查（编译不执行）。
